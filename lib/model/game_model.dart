class GameModel {
  final String gameUrl, thumbnailUrl, gameTitle, gameDesc, gameType;

  GameModel({
    required this.gameTitle,
    required this.gameUrl,
    required this.thumbnailUrl,
    required this.gameDesc,
    required this.gameType,
  });
}
